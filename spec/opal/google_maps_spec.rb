require "spec_helper"

RSpec.describe Opal::GoogleMaps do
  it "has a version number" do
    expect(Opal::GoogleMaps::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
